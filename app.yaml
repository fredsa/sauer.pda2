# https://cloud.google.com/appengine/docs/standard/reference/app-yaml?tab=go
runtime: go123

# https://cloud.google.com/appengine/docs/standard/reference/app-yaml?tab=go#runtime_and_app_elements
app_engine_apis: on

# builtins:
# # Enable '/_ah/stats'
# - appstats: on
# # Redirect `/_ah/admin` to Cloud Console
# - admin_redirect: on
# #- remote_api: on

# # https://cloud.google.com/appengine/docs/standard/services/overview?tab=go
# inbound_services:
# - mail

handlers:
- url: /favicon\.ico
  static_files: favicon.ico
  upload: favicon\.ico
  login: admin
  secure: always

# - url: /stats.*
#   script: google.appengine.ext.appstats.ui.app
#   login: admin

# - url: /_ah/mail/.+
#   script: main.application
#   login: admin

# - url: /.*
#   script: main.application
#   login: admin
#   secure: always

- url: /.*
  script: auto
  login: admin
  secure: always
